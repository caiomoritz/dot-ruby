#+TITLE:    My .pryrc config file
#+AUTHOR:   Caio Ronchi
#+EMAIL:    ronchi.work@posteo.de
#+PROPERTY: header-args:ruby :tangle (file-name-sans-extension (buffer-file-name))
#+STARTUP:  fold

#+begin_src ruby :exports none
# DO NOT EDIT THIS FILE DIRECTLY
# This is a file generated from a literate programing source file located at
# https://github.com/caiomoritz/dot-ruby/-/blob/master/.pryrc.org
# You can always recreate it from within .pryrc.org using C-c C-v t (org-babel-tangle)
#+end_src

* Aliases

The following shortcuts aren't included by default since these symbols
may conflict with the local scope.

#+begin_src ruby

  if defined?(PryByebug)
    Pry.commands.alias_command 'c', 'continue'
    Pry.commands.alias_command 's', 'step'
    Pry.commands.alias_command 'n', 'next'
    Pry.commands.alias_command 'f', 'finish'
  end

#+end_src
